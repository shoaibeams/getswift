import colors from './colors';

export const customHeader = () => {
  return {
    headerLeft: null,
    headerStyle: {
      backgroundColor: colors.PRIMARY,
    },
    headerTitleStyle: {
      color: colors.WHITE,
    },
  };
};
